import 'package:flutter/material.dart';

void main(){

  var app = MaterialApp(
      title: 'Piyush App',
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        appBar: AppBar(),
      )
  );

  runApp(app);
}